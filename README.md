# cordova-plugin-hide-keyboard
Cordova plugin to hide soft keyboard

## Add to Cordova project
Use `cordova plugin add` (recommended) or `plugman`

If having plugin checkout locally, use `--link` if you want to develop it.

```sh
npx cordova plugin add ../cordova-plugin-hide-keyboard/HideKeyboardPlugin/ --link 
```